# Go_practise

This repository is used for store some Go project,

the structure of the repo is as followed:

```bash

```

## How to run

type in your command-line:

```bash
cd target-dir
go run . command
```
